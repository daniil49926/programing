/*#ifndef sin

#define sin
#include"fact.h"
#include<iostream>

double sin(double i, int x) {
	int i;
	int x;
	double res = 0;
	for (int i = 0; i < k; i++) {
		res += pow(-1, i) * (pow(x, (2 * i + 1)) / fact(2 * i + 1));
	}
	return res;



#endif // !sin

*/